import React, { Fragment } from 'react';
import spinner from '../media/spinner.gif';

const Spinner = () => {
   <Fragment>
      <img src={spinner} alt="Loading..." style={{ wdith: '200px', margin: 'auto', display: 'block' }}/>
   </Fragment>
}

export default Spinner